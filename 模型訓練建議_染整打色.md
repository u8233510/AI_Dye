# 染整打色 AI 模型訓練建議（依你們目前流程）

## 1) 問題定義先拆成兩段（最實務）

你們現在流程是：客戶給布種/規格 + 目標色 L
a
b → 你們配方（染料種類與用量）→ 打色 → Datacolor 量測 ΔL、Δa、Δb、CMC DE。

這個問題建議拆成兩個模型：

1. **Forward Model（正向模型）**
   - 輸入：布種規格、前處理、染料配方（每支染料濃度/用量）、機台與製程條件
   - 輸出：預測量測後 L\*a\*b\*（或直接預測 ΔL、Δa、Δb）

2. **Recipe Generator（反向建議）**
   - 目標：給定客戶目標 L\*a\*b\*，自動找出「可做、可染、可重現」的配方
   - 作法：先用 Forward Model 當「模擬器」，再用最佳化演算法找配方

> 核心原因：直接用單一模型從目標色「一次預測配方」通常不穩定，因為同一顏色可能對應多組配方（多解問題）。

---

## 2) 你們這類 CSV 明細資料，優先選的模型

如果資料是典型工廠明細（欄位多為數值 + 類別，樣本量中小型），**第一優先建議：樹模型（Gradient Boosting）**。

### 最推薦起手式

- **CatBoost / LightGBM / XGBoost（多輸出回歸）**
  - 優點：
    - 對表格資料非常強
    - 對非線性、欄位交互效果好
    - 不需要超大量資料也能有不錯效果
    - 類別欄位處理容易（尤其 CatBoost）
  - 適合你們目前「配方 + 製程 → Lab/Δ值」預測

### 次選

- **Random Forest / ExtraTrees**
  - 好處是穩定、易用
  - 但極限精度通常低於 boosting

### 深度學習何時再用

- **MLP/TabTransformer** 僅在以下情況才考慮：
  - 資料量非常大（例如 >10萬筆且欄位工程完整）
  - 你們要做跨廠別、跨機台的大規模泛化

---

## 3) 指標要直接對齊你們驗收標準

你們已定義：
- |ΔL| 盡量 < 0.5
- |Δa|、|Δb| 越小越好，且要同象限
- CMC DE < 0.8

建議訓練與驗證同時看：

1. **回歸誤差**：MAE on ΔL、Δa、Δb
2. **達標率（最重要）**：
   - Pass@DL：|ΔL| ≤ 0.5 的比例
   - Pass@CMC：CMC DE ≤ 0.8 的比例
   - Pass@All：同時滿足上述條件的比例
3. **象限一致率**：
   - sign(Δa_pred) 與 sign(Δa_true) 一致率
   - sign(Δb_pred) 與 sign(Δb_true) 一致率

> 你們最終不是在比 RMSE，而是在比「一次打色就過關」的比例。

---

## 4) 反向配方（給客戶目標色）建議做法

1. 用 Forward Model 預測某配方會得到的 ΔL、Δa、Δb、CMC
2. 設計一個目標函數：

\[
Loss = w_1|ΔL| + w_2|Δa| + w_3|Δb| + w_4\,CMC + Penalty_{quadrant} + Penalty_{process}
\]

3. 用最佳化演算法找最小 Loss 的配方：
   - 建議先用 **Bayesian Optimization** 或 **遺傳演算法（GA）**
4. 加入實務限制：
   - 染料上下限
   - 染料總量上限
   - 最小加料步階（例如 0.01%、0.05%）
   - 禁配組合（不相容染料）

---

## 5) 資料欄位最少要有什麼（CSV 建議）

要讓模型真的可用，CSV 最少包含：

- 工單/批號、日期、機台、操作員（可匿名）
- 布種、纖維成分、克重、組織、前處理條件
- 每支染料 ID 與實際用量
- 浴比、溫度曲線、時間、pH、助劑
- 客戶目標 L\*a\*b\*
- 實測 L\*a\*b\*（Datacolor）
- 計算後 ΔL、Δa、Δb、CMC DE
- 是否一次通過（標記 0/1）

如果缺「製程條件」只留配方，模型通常會有天花板。

---

## 6) 依資料量給你一個可落地決策

- **< 3,000 筆**：CatBoost 優先，重點做特徵工程與清洗
- **3,000–30,000 筆**：CatBoost/LightGBM + 嚴謹交叉驗證（Group by 布種或客戶）
- **> 30,000 筆**：可加入更複雜模型（堆疊、淺層神經網路）

---

## 7) 建議你們的第一版專案目標

1. 先做 Forward Model，目標是把 **Pass@All** 拉高
2. 再做配方最佳化器（非直接端到端）
3. 線上部署時先做「建議 Top-3 配方」，讓打樣師選
4. 持續回灌 Datacolor 新資料，每月重訓

---


## 8) 你問「CatBoost / LightGBM / XGBoost 先做哪一個？」

**建議先做 CatBoost。**

原因（依你們情境排序）：

1. 你們資料很可能有不少類別欄位（布種、染料代碼、機台、客戶、工單類型），CatBoost 對類別特徵處理更直接，前處理成本低。
2. 在中小型表格資料上，CatBoost 通常能在「少調參」下得到穩定表現，適合先做可用版本。
3. 你們重視的是一次打色達標率（Pass@All、同象限），CatBoost 常能提供不錯的泛化穩定度。

實作建議：
- **Step 1（先上線）**：CatBoost 做多輸出（或分三個模型預測 ΔL/Δa/Δb）
- **Step 2（再比較）**：用同一份切分與指標，對照 LightGBM / XGBoost
- **Step 3（定版）**：以 Pass@All 與 CMC 達標率最高者為主模型

若你要一個最簡短決策：

> 先做 **CatBoost**；再用 LightGBM、XGBoost 當 challenger 做 A/B 比較。

---

## 結論（直接回答你問的）

以你們的資料型態（CSV 明細、配方與製程欄位、要預測 Lab/Δ值與 CMC），**最適合先訓練的 AI 模型是 CatBoost/LightGBM/XGBoost 這類 Gradient Boosting 的多輸出回歸模型**，並搭配**反向最佳化（Bayesian Optimization 或 GA）**去產生配方。

這樣最能對齊你們實際 KPI：
- |ΔL| < 0.5
- 同象限
- CMC DE < 0.8
- 一次打色通過率提升
